#include <iostream>
using namespace std;

int main(){
    int n;
    cin >> n;
    string s;
    cin >> s;
    char temp = a;
    int *arr = new int[n];
    for(int i = 0; i < n; i++){
        cin >> arr[i];
    }   
    for(int i = 0; i < s.length(); i++){
        if(s[i] > 64 && s[i] < 91){
            
        }
    }
}
